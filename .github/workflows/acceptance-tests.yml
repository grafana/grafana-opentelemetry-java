name: Acceptance Tests label trigger

on:
  pull_request:
    branches:
      - main
    types:
      - labeled
      - opened
      - synchronize
      - reopened

jobs:
  acceptance-tests:
    uses: ./.github/workflows/reusable-acceptance-tests.yml
